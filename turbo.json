{
  "extends": ["//"],
  "tasks": {
    "test": {
      "outputs": []
    },
    "generate": {
      "inputs": ["src/**/*.json", "scripts/**/*.ts"]
    },
    "build": {
      "dependsOn": [],
      "inputs": ["src/**/*.ts", "build.ts"],
      "outputs": ["dist/**"]
    }
  }
}
