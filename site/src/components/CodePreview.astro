<section class="code-section">
  <div class="code-container">
    <div class="code-grid">
      <!-- For Users -->
      <div class="code-panel">
        <div class="code-panel-inner">
          <div class="code-badge">For Users</div>
          <h3>Drop-in SDK</h3>
          <p>
            Integrate easily with standard fetch wrappers or axios interceptors. No need to manage sessions or tokens manually in your frontend code.
          </p>

          <div class="code-block-wrapper">
            <pre><code><span class="kw">import</span> {"{"} <span class="fn">createClient</span> {"}"} <span class="kw">from</span> <span class="str">'@slicekit/erc8128'</span>
<span class="kw">import</span> {"{"} <span class="fn">privateKeyToAccount</span> {"}"} <span class="kw">from</span> <span class="str">'viem/accounts'</span>

<span class="kw">const</span> account = <span class="fn">privateKeyToAccount</span>(<span class="str">'0x...'</span>)
<span class="kw">const</span> client = <span class="fn">createClient</span>({"{"}
  chainId: <span class="num">1</span>,
  address: account.address,
  signMessage: (msg) =&gt; account.<span class="fn">signMessage</span>({"{"} message: {"{"} raw: msg {"}"} {"}"}),
{"}"})

<span class="kw">const</span> response = <span class="kw">await</span> client.<span class="fn">fetch</span>(
  <span class="str">'https://api.example.com/orders'</span>, {"{"}<br />  method: <span class="str">'POST'</span>,
  body: <span class="fn">JSON.stringify</span>({"{"} amount: <span class="str">'100'</span> {"}"}),
{"}"})</code></pre>
          </div>
        </div>
      </div>

      <!-- For Providers -->
      <div class="code-panel code-panel-dark">
        <div class="code-panel-inner">
          <div class="code-badge">For Providers</div>
          <h3>Stateless Verify</h3>
          <p>
            Middleware handles the heavy cryptography. You get a validated wallet address in the request context without database lookups.
          </p>

          <div class="code-block-wrapper">
            <pre><code><span class="kw">import</span> {"{"} <span class="fn">verifyRequest</span> {"}"} <span class="kw">from</span> <span class="str">'@slicekit/erc8128'</span>
<span class="kw">import</span> {"{"} <span class="fn">createPublicClient</span>, <span class="fn">http</span> {"}"} <span class="kw">from</span> <span class="str">'viem'</span>
<span class="kw">import</span> {"{"} mainnet {"}"} <span class="kw">from</span> <span class="str">'viem/chains'</span>

<span class="kw">const</span> viemClient = <span class="fn">createPublicClient</span>({"{"}<br />  chain: mainnet,
  transport: <span class="fn">http</span>(),
{"}"})

<span class="kw">const</span> result = <span class="kw">await</span> <span class="fn">verifyRequest</span>(request, {"{"}<br />  nonceStore,
  verifyMessage: viemClient.verifyMessage,
{"}"})

<span class="kw">if</span> (result.ok) {"{"}<br />  console.<span class="fn">log</span>(<span class="str">`Authenticated: <span class="kw">${"{"}</span>result.address<span class="kw">{"}"}</span>`</span>)
{"}"}</code></pre>
          </div>
        </div>
      </div>
    </div>

    <!-- Install bar -->
    <div class="install-bar">
      <code><span class="install-prefix">$</span> npm install @slicekit/erc8128</code>
      <button class="copy-btn" title="Copy install command" aria-label="Copy install command">
        <svg
          width="16"
          height="16"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
        >
          <rect x="9" y="9" width="13" height="13" rx="2"></rect>
          <path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1"></path>
        </svg>
      </button>
    </div>
  </div>
</section>

<style>
  .code-section {
    border-bottom: 1px solid var(--border);
  }

  .code-container {
    max-width: 80rem;
    margin: 0 auto;
  }

  .code-grid {
    display: flex;
    flex-direction: column;
  }

  @media (min-width: 1024px) {
    .code-grid {
      flex-direction: row;
    }
  }

  .code-panel {
    flex: 1;
    border-bottom: 1px solid var(--border);
    display: flex;
    flex-direction: column;
  }

  @media (min-width: 1024px) {
    .code-panel {
      border-bottom: none;
      border-right: 1px solid var(--border);
    }

    .code-panel:last-child {
      border-right: none;
    }
  }

  .code-panel-dark {
    background: var(--surface);
  }

  .code-panel-inner {
    padding: 2rem;
    flex-grow: 1;
  }

  @media (min-width: 1024px) {
    .code-panel-inner {
      padding: 4rem;
    }
  }

  .code-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    border: 1px solid var(--border);
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-bottom: 2rem;
  }

  h3 {
    font-size: 1.875rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
  }

  .code-section p {
    opacity: 0.7;
    max-width: 28rem;
    line-height: 1.6;
    margin-bottom: 2.5rem;
  }

  .code-block-wrapper {
    background: #0a0a0a;
    border: 1px solid rgba(255, 255, 255, 0.2);
    padding: 1.5rem;
    overflow-x: auto;
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
  }

  .code-panel-dark .code-block-wrapper {
    background: #000000;
  }

  .code-block-wrapper pre {
    font-family: 'JetBrains Mono', 'Fira Code', 'SF Mono', monospace;
    font-size: 13px;
    line-height: 1.75;
    color: rgba(255, 255, 255, 0.6);
  }

  .code-block-wrapper :global(.kw) { color: #c084fc; }
  .code-block-wrapper :global(.fn) { color: #60a5fa; }
  .code-block-wrapper :global(.str) { color: #34d399; }
  .code-block-wrapper :global(.cm) { color: #4a4a50; }
  .code-block-wrapper :global(.ty) { color: #f472b6; }
  .code-block-wrapper :global(.num) { color: #fbbf24; }

  .install-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: #0a0a0a;
    border-top: 1px solid rgba(255, 255, 255, 0.2);
    padding: 1.125rem 1.5rem;
    font-family: 'JetBrains Mono', 'Fira Code', 'SF Mono', monospace;
    font-size: 0.875rem;
    transition: background 0.3s ease;
  }

  .install-bar:hover {
    background: #111;
  }

  .install-bar code {
    color: rgba(255, 255, 255, 0.6);
  }

  .install-prefix {
    color: #858aff;
    margin-right: 0.5rem;
  }

  .copy-btn {
    background: none;
    border: none;
    color: rgba(255, 255, 255, 0.5);
    cursor: pointer;
    padding: 0.375rem;
    border-radius: 6px;
    transition: all 0.25s ease;
    display: flex;
    align-items: center;
  }

  .copy-btn:hover {
    color: #858aff;
    background: rgba(133, 138, 255, 0.1);
  }
</style>

<script>
  // Copy install command with toast
  document.querySelector(".copy-btn")?.addEventListener("click", () => {
    navigator.clipboard.writeText("npm install @slicekit/erc8128");
    const toast = document.getElementById("toast");
    if (toast) {
      toast.classList.add("show");
      setTimeout(() => toast.classList.remove("show"), 2000);
    }
    const btn = document.querySelector(".copy-btn");
    if (btn) {
      const originalHTML = btn.innerHTML;
      btn.innerHTML = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#858AFF" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>`;
      setTimeout(() => { btn.innerHTML = originalHTML; }, 2000);
    }
  });
</script>
