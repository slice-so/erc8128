<header class="hero">
  <div class="hero-left">
    <div>
      <div class="accent-bar"></div>
      <div class="hero-h1" aria-hidden="true">ERC<br />8128</div>
    </div>

    <div class="hero-bottom">
      <p class="hero-subtitle">
        Ethereum HTTP<br />
        Authentication<br />
        Standard
      </p>
      <div class="hero-ctas">
        <a
          href="https://erc8128-docs.vercel.app/"
          class="hero-cta hero-cta-primary"
          target="_blank"
          rel="noopener noreferrer"
        >
          GET STARTED <span class="cta-arrow">→</span>
        </a>
        <a
          href="https://github.com/ethereum/ERCs/pull/1478"
          class="hero-cta hero-cta-secondary"
          target="_blank"
          rel="noopener noreferrer"
        >
          VIEW SPECIFICATION <span class="cta-arrow">↗</span>
        </a>
      </div>
      <div class="accent-bar accent-bar-bottom"></div>
    </div>
  </div>

  <div class="hero-globe">
    <canvas id="dots"></canvas>
  </div>
</header>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1fr;
    border-bottom: 1px solid var(--border);
    min-height: 85vh;
    position: relative;
  }

  @media (min-width: 768px) {
    .hero {
      grid-template-columns: 1fr auto;
    }
  }

  @media (min-width: 1024px) {
    .hero {
      grid-template-columns: 40% 60%;
    }
  }

  .hero-left {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    position: relative;
    z-index: 2;
  }

  @media (min-width: 768px) {
    .hero-left {
      padding: 3rem;
    }
  }

  @media (min-width: 1024px) {
    .hero-left {
      padding: 4rem;
      border-right: 1px solid var(--border);
    }
  }

  .accent-bar {
    width: 120px;
    height: 6px;
    background: white;
    margin-bottom: 2rem;
    margin-top: 1rem;
  }

  @media (max-width: 767px) {
    .accent-bar {
      margin-top: 1.5rem;
    }
  }

  .accent-bar-bottom {
    margin-top: 2rem;
    margin-bottom: 0;
  }

  /* ── Hero CTAs (text-only style) ── */
  .hero-ctas {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    margin-top: 2.5rem;
  }

  .hero-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    font-weight: 800;
    font-size: clamp(1.125rem, 2.5vw, 1.5rem);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    text-decoration: none;
    transition: opacity 0.25s ease;
    line-height: 1;
  }

  .hero-cta:hover {
    opacity: 0.7;
  }

  .hero-cta-primary {
    color: white;
  }

  .hero-cta-secondary {
    color: rgba(255, 255, 255, 0.45);
  }

  .hero-cta-secondary:hover {
    color: rgba(255, 255, 255, 0.7);
    opacity: 1;
  }

  .cta-arrow {
    font-weight: 300;
    font-size: 1.3em;
    opacity: 0.8;
  }

  .hero-h1 {
    font-size: clamp(4rem, 10vw, 9rem);
    font-weight: 700;
    line-height: 0.9;
    letter-spacing: -0.04em;
    text-transform: uppercase;
  }

  .hero-subtitle {
    font-size: clamp(1.5rem, 3.5vw, 2.25rem);
    font-weight: 400;
    line-height: 1.25;
    opacity: 0.9;
    max-width: 28rem;
  }

  .hero-bottom {
    margin-top: 3rem;
  }

  @media (min-width: 1024px) {
    .hero-bottom {
      margin-top: 0;
    }
  }

  /* ── Globe: stays at hero level, fades on narrow screens ── */
  .hero-globe {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    width: 55%;
    overflow: hidden;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  @media (min-width: 768px) {
    .hero-globe {
      position: relative;
      width: auto;
      opacity: 0.4;
      pointer-events: auto;
    }
  }

  @media (min-width: 1024px) {
    .hero-globe {
      opacity: 1;
    }
  }

  #dots {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }
</style>

<script>
  const canvas = document.getElementById('dots') as HTMLCanvasElement;
  const ctx = canvas.getContext('2d')!;

  let width = 0, height = 0;
  let dpr = 1;
  let angleX = 0.3;
  let angleY = 0;
  let targetAngleX = 0.3;
  let targetAngleY = 0;
  let autoRotateY = 0;
  let isDragging = false;
  let lastMouseX = 0;
  let lastMouseY = 0;
  let mouseX = -9999;
  let mouseY = -9999;
  let velocityX = 0;
  let velocityY = 0;

  function resize() {
    const parent = canvas.parentElement!;
    width = parent.offsetWidth;
    height = parent.offsetHeight;
    dpr = window.devicePixelRatio || 1;
    canvas.width = width * dpr;
    canvas.height = height * dpr;
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
  }

  // ── Generate points on a sphere — Fibonacci distribution ──
  // More points = tighter spacing, denser network
  const NUM_POINTS = 280;
  const spherePoints: { x: number; y: number; z: number }[] = [];
  const goldenAngle = Math.PI * (3 - Math.sqrt(5));

  for (let i = 0; i < NUM_POINTS; i++) {
    const y = 1 - (i / (NUM_POINTS - 1)) * 2;
    const radius = Math.sqrt(1 - y * y);
    const theta = goldenAngle * i;
    spherePoints.push({
      x: Math.cos(theta) * radius,
      y: y,
      z: Math.sin(theta) * radius,
    });
  }

  // Connection threshold — balanced: visible network without becoming a blob
  const CONNECTION_THRESHOLD = 0.38;

  // Pre-compute neighbor pairs (unit sphere distance)
  const pairs: [number, number][] = [];
  for (let i = 0; i < NUM_POINTS; i++) {
    for (let j = i + 1; j < NUM_POINTS; j++) {
      const dx = spherePoints[i].x - spherePoints[j].x;
      const dy = spherePoints[i].y - spherePoints[j].y;
      const dz = spherePoints[i].z - spherePoints[j].z;
      const dist = Math.sqrt(dx * dx + dy * dy + dz * dz);
      if (dist < CONNECTION_THRESHOLD) {
        pairs.push([i, j]);
      }
    }
  }

  // ── Build adjacency list for multi-hop signal paths ──
  const adjacency: number[][] = Array.from({ length: NUM_POINTS }, () => []);
  const pairByEdge = new Map<string, number>();  // "i-j" → pair index
  for (let pIdx = 0; pIdx < pairs.length; pIdx++) {
    const [i, j] = pairs[pIdx];
    adjacency[i].push(j);
    adjacency[j].push(i);
    pairByEdge.set(`${i}-${j}`, pIdx);
    pairByEdge.set(`${j}-${i}`, pIdx);
  }

  // ── Traveling signal animation ──
  // A signal travels along a multi-hop path from node A to node B
  interface TravelingSignal {
    path: number[];           // node indices forming the path
    edgeIndices: number[];    // pair indices for each edge in path
    currentEdge: number;      // which edge we're on (0-based)
    t: number;                // 0→1 progress along current edge
    speed: number;            // how fast t increases per frame
    tailLength: number;       // how many edges the glowing tail extends back
    opacity: number;          // max opacity of the signal head
  }

  const signals: TravelingSignal[] = [];
  const MAX_SIGNALS = 6;

  // Find a random multi-hop path using BFS with random walk
  function findRandomPath(minHops: number, maxHops: number): number[] | null {
    const start = Math.floor(Math.random() * NUM_POINTS);
    const path = [start];
    const visited = new Set([start]);
    let current = start;

    const targetHops = minHops + Math.floor(Math.random() * (maxHops - minHops + 1));

    for (let step = 0; step < targetHops; step++) {
      const neighbors = adjacency[current].filter(n => !visited.has(n));
      if (neighbors.length === 0) break;
      const next = neighbors[Math.floor(Math.random() * neighbors.length)];
      path.push(next);
      visited.add(next);
      current = next;
    }

    return path.length >= minHops + 1 ? path : null;
  }

  function spawnSignal() {
    if (signals.length >= MAX_SIGNALS) return;

    const path = findRandomPath(3, 7);
    if (!path) return;

    // Build edge indices for the path
    const edgeIndices: number[] = [];
    for (let i = 0; i < path.length - 1; i++) {
      const key = `${path[i]}-${path[i + 1]}`;
      const altKey = `${path[i + 1]}-${path[i]}`;
      const pIdx = pairByEdge.get(key) ?? pairByEdge.get(altKey);
      if (pIdx === undefined) return; // shouldn't happen
      edgeIndices.push(pIdx);
    }

    signals.push({
      path,
      edgeIndices,
      currentEdge: 0,
      t: 0,
      speed: 0.012 + Math.random() * 0.012,
      tailLength: 2 + Math.floor(Math.random() * 2),
      opacity: 0.6 + Math.random() * 0.3,
    });
  }

  // Seed initial signals with staggered starts
  for (let i = 0; i < 4; i++) {
    spawnSignal();
    // Stagger: advance some signals partway through their path
    if (signals.length > 0) {
      const s = signals[signals.length - 1];
      const advanceEdges = Math.floor(Math.random() * Math.min(3, s.edgeIndices.length));
      s.currentEdge = advanceEdges;
      s.t = Math.random();
    }
  }

  // ── Build a lookup: for each pair index, what signal data applies? ──
  // We'll rebuild this each frame
  interface EdgeGlow {
    intensity: number;  // 0-1, how bright this edge should glow
    isHead: boolean;    // is the signal head on this edge?
    headT: number;      // if isHead, where along the edge (0-1)
    fromNode: number;   // direction: signal travels from → to
    toNode: number;
  }

  const edgeGlowMap = new Map<number, EdgeGlow>();

  // Mouse / touch interaction
  const heroGlobe = canvas.parentElement!;

  heroGlobe.addEventListener('mousedown', (e) => {
    isDragging = true;
    lastMouseX = e.clientX;
    lastMouseY = e.clientY;
    velocityX = 0;
    velocityY = 0;
  });

  window.addEventListener('mousemove', (e) => {
    const rect = heroGlobe.getBoundingClientRect();
    mouseX = e.clientX - rect.left;
    mouseY = e.clientY - rect.top;

    if (isDragging) {
      const dx = e.clientX - lastMouseX;
      const dy = e.clientY - lastMouseY;
      velocityY = -dx * 0.005;
      velocityX = -dy * 0.005;
      targetAngleY -= dx * 0.005;
      targetAngleX -= dy * 0.005;
      lastMouseX = e.clientX;
      lastMouseY = e.clientY;
    }
  });

  window.addEventListener('mouseup', () => {
    isDragging = false;
  });

  heroGlobe.addEventListener('mouseleave', () => {
    mouseX = -9999;
    mouseY = -9999;
  });

  // Touch support
  heroGlobe.addEventListener('touchstart', (e) => {
    isDragging = true;
    const t = e.touches[0];
    lastMouseX = t.clientX;
    lastMouseY = t.clientY;
    velocityX = 0;
    velocityY = 0;
  }, { passive: true });

  heroGlobe.addEventListener('touchmove', (e) => {
    const t = e.touches[0];
    const rect = heroGlobe.getBoundingClientRect();
    mouseX = t.clientX - rect.left;
    mouseY = t.clientY - rect.top;

    if (isDragging) {
      const dx = t.clientX - lastMouseX;
      const dy = t.clientY - lastMouseY;
      velocityY = -dx * 0.005;
      velocityX = -dy * 0.005;
      targetAngleY -= dx * 0.005;
      targetAngleX -= dy * 0.005;
      lastMouseX = t.clientX;
      lastMouseY = t.clientY;
    }
  }, { passive: true });

  heroGlobe.addEventListener('touchend', () => {
    isDragging = false;
    mouseX = -9999;
    mouseY = -9999;
  });

  heroGlobe.style.cursor = 'grab';

  // Easing function for smooth signal motion
  function easeInOutCubic(t: number): number {
    return t < 0.5 ? 4 * t * t * t : 1 - Math.pow(-2 * t + 2, 3) / 2;
  }

  let lastTime = 0;

  function draw(time: number) {
    const dt = lastTime ? Math.min((time - lastTime) / 16.67, 3) : 1;
    lastTime = time;

    if (!width || !height) {
      requestAnimationFrame(draw);
      return;
    }

    ctx.clearRect(0, 0, width, height);

    const cx = width / 2;
    const cy = height / 2;
    const sphereRadius = Math.min(width, height) * 0.38;
    const cursorRadius = sphereRadius * 0.35;

    // Auto-rotation
    if (!isDragging) {
      autoRotateY += 0.0015 * dt;
      targetAngleY += velocityY * dt;
      targetAngleX += velocityX * dt;
      velocityX *= 0.96;
      velocityY *= 0.96;
    }

    angleX += (targetAngleX - angleX) * 0.1 * dt;
    angleY += (targetAngleY - angleY) * 0.1 * dt;

    const totalAngleY = angleY + autoRotateY;
    const cosY = Math.cos(totalAngleY);
    const sinY = Math.sin(totalAngleY);
    const cosX = Math.cos(angleX);
    const sinX = Math.sin(angleX);

    // Project all points
    const projected: { px: number; py: number; z: number; idx: number }[] = [];

    for (let i = 0; i < spherePoints.length; i++) {
      const p = spherePoints[i];
      const x1 = p.x * cosY - p.z * sinY;
      const z1 = p.x * sinY + p.z * cosY;
      const y1 = p.y;
      const y2 = y1 * cosX - z1 * sinX;
      const z2 = y1 * sinX + z1 * cosX;

      projected.push({
        px: cx + x1 * sphereRadius,
        py: cy + y2 * sphereRadius,
        z: z2,
        idx: i,
      });
    }

    // ── Update traveling signals ──
    edgeGlowMap.clear();

    for (let si = signals.length - 1; si >= 0; si--) {
      const sig = signals[si];
      sig.t += sig.speed * dt;

      // Advance to next edge when t >= 1
      while (sig.t >= 1 && sig.currentEdge < sig.edgeIndices.length - 1) {
        sig.t -= 1;
        sig.currentEdge++;
      }

      // Signal finished its path
      if (sig.t >= 1 && sig.currentEdge >= sig.edgeIndices.length - 1) {
        signals.splice(si, 1);
        continue;
      }

      // Apply glow to current edge (head) and trailing edges (tail)
      for (let tailOffset = 0; tailOffset <= sig.tailLength; tailOffset++) {
        const edgeIdx = sig.currentEdge - tailOffset;
        if (edgeIdx < 0) break;

        const pairIdx = sig.edgeIndices[edgeIdx];
        // Intensity: head is brightest, tail fades out
        let intensity: number;
        if (tailOffset === 0) {
          // Head edge: full intensity modulated by eased position
          intensity = sig.opacity;
        } else {
          // Tail: fade based on distance from head
          const falloff = 1 - (tailOffset / (sig.tailLength + 1));
          intensity = sig.opacity * falloff * 0.6;
        }

        const existing = edgeGlowMap.get(pairIdx);
        if (!existing || existing.intensity < intensity) {
          edgeGlowMap.set(pairIdx, {
            intensity,
            isHead: tailOffset === 0,
            headT: tailOffset === 0 ? easeInOutCubic(sig.t) : -1,
            fromNode: sig.path[edgeIdx],
            toNode: sig.path[edgeIdx + 1],
          });
        }
      }
    }

    // Spawn new signals at staggered intervals
    if (Math.random() < 0.02 * dt) spawnSignal();

    // ── Draw connections (lines) ──
    for (let pIdx = 0; pIdx < pairs.length; pIdx++) {
      const [i, j] = pairs[pIdx];
      const a = projected[i];
      const b = projected[j];

      const avgZ = (a.z + b.z) / 2;
      const depthFactor = (avgZ + 1) / 2;

      // Base line: visible wireframe
      const baseOpacity = 0.04 + depthFactor * 0.08;

      // Cursor proximity boost
      let cursorBoost = 0;
      const midX = (a.px + b.px) / 2;
      const midY = (a.py + b.py) / 2;
      const distToCursor = Math.sqrt(
        (midX - mouseX) ** 2 + (midY - mouseY) ** 2
      );
      if (distToCursor < cursorRadius) {
        cursorBoost = (1 - distToCursor / cursorRadius) * 0.12;
      }

      const glow = edgeGlowMap.get(pIdx);

      if (glow && glow.intensity > 0.01) {
        // Draw the base line first (dim)
        const dimOpacity = Math.min(baseOpacity + cursorBoost, 0.2);
        ctx.strokeStyle = `rgba(255, 255, 255, ${dimOpacity})`;
        ctx.lineWidth = 0.5 + depthFactor * 0.5;
        ctx.beginPath();
        ctx.moveTo(a.px, a.py);
        ctx.lineTo(b.px, b.py);
        ctx.stroke();

        // Determine direction: which projected point is "from" and which is "to"
        const fromPt = projected[glow.fromNode] || a;
        const toPt = projected[glow.toNode] || b;

        if (glow.isHead && glow.headT >= 0) {
          // ── Draw traveling signal as a gradient along the edge ──
          const glowIntensity = glow.intensity * depthFactor;
          const headX = fromPt.px + (toPt.px - fromPt.px) * glow.headT;
          const headY = fromPt.py + (toPt.py - fromPt.py) * glow.headT;

          // Signal trail: from slightly behind head to head position
          const trailStart = Math.max(0, glow.headT - 0.4);
          const trailX = fromPt.px + (toPt.px - fromPt.px) * trailStart;
          const trailY = fromPt.py + (toPt.py - fromPt.py) * trailStart;

          // Gradient from trail to head
          const grad = ctx.createLinearGradient(trailX, trailY, headX, headY);
          grad.addColorStop(0, `rgba(255, 255, 255, 0)`);
          grad.addColorStop(0.7, `rgba(255, 255, 255, ${glowIntensity * 0.5})`);
          grad.addColorStop(1, `rgba(255, 255, 255, ${glowIntensity})`);

          ctx.strokeStyle = grad;
          ctx.lineWidth = 1.2 + depthFactor * 1.8;
          ctx.beginPath();
          ctx.moveTo(trailX, trailY);
          ctx.lineTo(headX, headY);
          ctx.stroke();

          // Small bright dot at signal head
          const dotRadius = 1.5 + depthFactor * 2;
          ctx.fillStyle = `rgba(255, 255, 255, ${Math.min(glowIntensity * 1.2, 1)})`;
          ctx.beginPath();
          ctx.arc(headX, headY, dotRadius, 0, Math.PI * 2);
          ctx.fill();

          // Subtle glow halo around head
          const haloGrad = ctx.createRadialGradient(headX, headY, 0, headX, headY, dotRadius * 4);
          haloGrad.addColorStop(0, `rgba(255, 255, 255, ${glowIntensity * 0.3})`);
          haloGrad.addColorStop(1, `rgba(255, 255, 255, 0)`);
          ctx.fillStyle = haloGrad;
          ctx.beginPath();
          ctx.arc(headX, headY, dotRadius * 4, 0, Math.PI * 2);
          ctx.fill();

        } else {
          // Tail edge: uniform glow fading out
          const tailIntensity = glow.intensity * depthFactor;
          ctx.strokeStyle = `rgba(255, 255, 255, ${tailIntensity})`;
          ctx.lineWidth = 0.8 + depthFactor * 1.2;
          ctx.beginPath();
          ctx.moveTo(fromPt.px, fromPt.py);
          ctx.lineTo(toPt.px, toPt.py);
          ctx.stroke();
        }

      } else {
        // No glow — just draw base line
        const finalOpacity = Math.min(baseOpacity + cursorBoost, 0.2);
        ctx.strokeStyle = `rgba(255, 255, 255, ${finalOpacity})`;
        ctx.lineWidth = 0.5 + depthFactor * 0.5;
        ctx.beginPath();
        ctx.moveTo(a.px, a.py);
        ctx.lineTo(b.px, b.py);
        ctx.stroke();
      }
    }

    // Sort by z for depth ordering (back to front)
    const sortedProjected = [...projected].sort((a, b) => a.z - b.z);

    // ── Draw nodes ──
    for (const pt of sortedProjected) {
      const depthFactor = (pt.z + 1) / 2;
      let opacity = 0.15 + depthFactor * 0.7;
      let dotSize = 1.2 + depthFactor * 2;

      // Cursor proximity effect
      const distToCursor = Math.sqrt(
        (pt.px - mouseX) ** 2 + (pt.py - mouseY) ** 2
      );
      if (distToCursor < cursorRadius) {
        const proximity = 1 - distToCursor / cursorRadius;
        dotSize += proximity * 2.5;
        opacity = Math.min(opacity + proximity * 0.4, 1);
      }

      ctx.fillStyle = `rgba(255, 255, 255, ${opacity})`;
      ctx.beginPath();
      ctx.arc(pt.px, pt.py, dotSize, 0, Math.PI * 2);
      ctx.fill();
    }

    requestAnimationFrame(draw);
  }

  window.addEventListener('resize', resize);
  setTimeout(() => { resize(); requestAnimationFrame(draw); }, 100);
</script>
