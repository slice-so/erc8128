<section class="hero">
  <div class="hero-glow"></div>
  <div class="container">
    <div class="badge animate-in">
      <span class="badge-dot"></span>
       Ethereum Standard · Draft
    </div>
    <h1 class="animate-in">Let users authenticate with<br />their Ethereum account</h1>
    <p class="animate-in">
      Zero signups. Zero API keys. Zero handshakes.<br class="hide-mobile" />
      Users sign requests directly&mdash;you verify the signature.
    </p>
    <div class="hero-ctas animate-in">
      <a
        href="https://erc8128-docs.vercel.app/"
        class="btn btn-primary"
        target="_blank"
        rel="noopener noreferrer"
      >
        Read the Docs
        <svg
          width="16"
          height="16"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
          ><path d="M5 12h14M12 5l7 7-7 7"></path></svg
        >
      </a>
      <a
        href="https://github.com/ethereum/ERCs/pull/1478"
        class="btn btn-secondary"
        target="_blank"
        rel="noopener noreferrer"
      >
        View Specification
        <svg
          width="14"
          height="14"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
          ><path
            d="M18 13v6a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2h6M15 3h6v6M10 14L21 3"
          ></path></svg
        >
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding-top: 180px;
    padding-bottom: 100px;
    text-align: center;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    top: -200px;
    left: 50%;
    transform: translateX(-50%);
    width: 800px;
    height: 600px;
    background: radial-gradient(
      ellipse at center,
      rgba(133, 138, 255, 0.08) 0%,
      rgba(133, 138, 255, 0.03) 40%,
      transparent 70%
    );
    pointer-events: none;
    animation: glowPulse 8s ease-in-out infinite alternate;
  }

  @keyframes glowPulse {
    0% { opacity: 0.6; transform: translateX(-50%) scale(1); }
    100% { opacity: 1; transform: translateX(-50%) scale(1.1); }
  }

  .badge {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 7px 16px;
    background: var(--accent-dim);
    border: 1px solid var(--accent-border);
    border-radius: 100px;
    font-size: 13px;
    color: var(--accent);
    font-weight: 500;
    margin-bottom: 36px;
  }

  .badge-dot {
    width: 6px;
    height: 6px;
    background: var(--accent);
    border-radius: 50%;
    animation: dotPulse 2s ease-in-out infinite;
  }

  @keyframes dotPulse {
    0%, 100% { opacity: 0.5; }
    50% { opacity: 1; }
  }

  h1 {
    font-size: clamp(38px, 6vw, 60px);
    font-weight: 800;
    line-height: 1.08;
    letter-spacing: -0.035em;
    margin-bottom: 24px;
    background: linear-gradient(180deg, #ffffff 0%, rgba(237, 237, 239, 0.65) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero p {
    font-size: 19px;
    color: var(--text-dim);
    max-width: 540px;
    margin: 0 auto 44px;
    line-height: 1.65;
  }

  .hero-ctas {
    display: flex;
    gap: 14px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 13px 26px;
    border-radius: 10px;
    font-size: 15px;
    font-weight: 600;
    transition: all 0.25s var(--ease);
    border: none;
    cursor: pointer;
    text-decoration: none;
  }

  .btn-primary {
    background: var(--accent);
    color: #fff;
    box-shadow: 0 0 0 0 rgba(133, 138, 255, 0);
  }
  .btn-primary:hover {
    background: var(--accent-hover);
    opacity: 1;
    box-shadow: 0 0 24px rgba(133, 138, 255, 0.3);
    transform: translateY(-1px);
  }

  .btn-secondary {
    background: var(--surface);
    color: var(--text);
    border: 1px solid var(--border);
  }
  .btn-secondary:hover {
    border-color: var(--text-dim);
    opacity: 1;
    background: var(--surface-elevated);
    transform: translateY(-1px);
  }

  .hide-mobile {
    display: inline;
  }

  @media (max-width: 640px) {
    .hero {
      padding-top: 140px;
      padding-bottom: 72px;
    }

    .hide-mobile {
      display: none;
    }

    .hero-glow {
      width: 500px;
      height: 400px;
    }
  }
</style>
