<section class="cta-section">
  <div class="container">
    <h2>Start signing requests today</h2>
    <p>
      Install the library, sign your first request, and verify it on your
      server—takes 5 minutes.
    </p>
    <div class="cta-buttons">
      <a
        href="https://erc8128-docs.vercel.app/"
        class="btn btn-primary"
        target="_blank"
        rel="noopener noreferrer"
      >
        Read the Docs
        <svg
          width="16"
          height="16"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
          ><path d="M5 12h14M12 5l7 7-7 7"></path></svg
        >
      </a>
      <a
        href="https://t.me/ethhttpmessagesignatures"
        class="btn btn-secondary"
        target="_blank"
        rel="noopener noreferrer"
      >
        💬 Join Telegram
      </a>
    </div>
  </div>
</section>

<style>
  .cta-section {
    text-align: center;
    padding: 60px 0 80px;
  }

  .cta-section h2 {
    font-size: clamp(24px, 4vw, 32px);
    font-weight: 800;
    letter-spacing: -0.02em;
    margin-bottom: 12px;
  }

  .cta-section p {
    color: var(--text-dim);
    margin-bottom: 32px;
    font-size: 16px;
  }

  .cta-buttons {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 24px;
    border-radius: 10px;
    font-size: 15px;
    font-weight: 600;
    transition: all 0.15s;
    border: none;
    cursor: pointer;
  }

  .btn-primary {
    background: var(--accent);
    color: #fff;
  }
  .btn-primary:hover {
    background: var(--accent-hover);
    opacity: 1;
  }

  .btn-secondary {
    background: var(--surface);
    color: var(--text);
    border: 1px solid var(--border);
  }
  .btn-secondary:hover {
    border-color: var(--text-dim);
    opacity: 1;
  }
</style>
