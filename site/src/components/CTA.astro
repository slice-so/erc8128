<section class="cta">
  <div class="cta-dots"></div>
  <div class="cta-inner">
    <h2>Integrate today</h2>
    <p>
      Install the library, sign your first request, and verify it on your server—takes 5 minutes.
    </p>
    <div class="cta-buttons">
      <a
        href="http://erc8128-docs.vercel.app/"
        class="cta-link cta-link-primary"
        target="_blank"
        rel="noopener noreferrer"
      >
        GET STARTED <span class="cta-arrow">→</span>
      </a>
      <a
        href="https://github.com/ethereum/ERCs/pull/1478"
        class="cta-link cta-link-secondary"
        target="_blank"
        rel="noopener noreferrer"
      >
        VIEW SPECIFICATION <span class="cta-arrow">↗</span>
      </a>
    </div>
  </div>
</section>

<style>
  .cta {
    border-bottom: 1px solid var(--border);
    padding: 8rem 1rem;
    text-align: center;
    background: var(--bg);
    position: relative;
    overflow: hidden;
  }

  .cta-dots {
    position: absolute;
    inset: 0;
    z-index: 0;
    opacity: 0.1;
    background-image: radial-gradient(circle, white 1px, transparent 1px);
    background-size: 30px 30px;
  }

  .cta-inner {
    position: relative;
    z-index: 10;
    max-width: 40rem;
    margin: 0 auto;
  }

  h2 {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 700;
    text-transform: uppercase;
    margin-bottom: 2rem;
    letter-spacing: -0.02em;
  }

  p {
    font-size: 1.125rem;
    opacity: 0.7;
    margin-bottom: 3rem;
  }

  .cta-buttons {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.25rem;
  }

  .cta-link {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    font-weight: 800;
    font-size: clamp(1rem, 2vw, 1.25rem);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    text-decoration: none;
    transition: opacity 0.25s ease;
    line-height: 1;
  }

  .cta-link:hover {
    opacity: 0.7;
  }

  .cta-link-primary {
    color: white;
  }

  .cta-link-secondary {
    color: rgba(255, 255, 255, 0.45);
  }

  .cta-link-secondary:hover {
    color: rgba(255, 255, 255, 0.7);
    opacity: 1;
  }

  .cta-arrow {
    font-weight: 300;
    font-size: 1.3em;
    opacity: 0.8;
  }
</style>
