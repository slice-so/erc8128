<footer>
  <div class="footer-top">
    <div class="footer-brand">
      <h2 class="brand-title">SLICE + ETHEREUM</h2>
      <div class="brand-initiative">OPEN STANDARDS INITIATIVE</div>
      <div class="brand-copyright">© 2024 SLICE SO. MIT LICENSE.</div>
    </div>
  </div>

  <div class="footer-bottom">
    <div class="footer-status">
      <div class="status-dot"></div>
      <span>STATUS: DRAFT</span>
    </div>
    <span class="footer-tagline">An open standard for HTTP authentication with Ethereum</span>
    <div class="footer-erc">
      <span>ERC-8128</span>
    </div>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid var(--border);
  }

  .footer-top {
    padding: 5rem 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .footer-brand {
    text-align: center;
  }

  .brand-title {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 600;
    letter-spacing: 0.08em;
    margin-bottom: 1rem;
    line-height: 1.2;
  }

  .brand-initiative {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: clamp(1rem, 2vw, 1.375rem);
    font-weight: 400;
    letter-spacing: 0.15em;
    opacity: 0.7;
    margin-bottom: 1.5rem;
  }

  .brand-copyright {
    font-family: var(--mono);
    font-size: 0.75rem;
    letter-spacing: 0.1em;
    opacity: 0.4;
  }

  .footer-bottom {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    padding: 1.5rem 2rem 2rem;
    font-size: 0.75rem;
    opacity: 0.6;
    font-family: var(--mono);
    border-top: 1px solid var(--border-subtle);
  }

  @media (min-width: 1024px) {
    .footer-bottom {
      flex-direction: row;
      justify-content: space-between;
      max-width: 56rem;
      margin: 0 auto;
      width: 100%;
    }
  }

  .footer-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .status-dot {
    width: 0.5rem;
    height: 0.5rem;
    background: white;
    border-radius: 9999px;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 0.5; }
    50% { opacity: 1; }
  }

  .footer-tagline {
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-align: center;
  }

  .footer-erc {
    display: flex;
    gap: 1.5rem;
  }
</style>
