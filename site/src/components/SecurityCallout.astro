<section class="border-b border-border py-24 px-8 md:py-32 md:px-12 relative bg-surface overflow-hidden">
  <!-- Grid background -->
  <div class="absolute inset-0 opacity-[0.04] pointer-events-none bg-grid-lg"></div>
  
  <div class="max-w-4xl mx-auto relative z-10">
    <div class="text-center mb-16">
      <h2 class="text-[clamp(2rem,5vw,3.5rem)] font-bold uppercase -tracking-[0.02em] mb-6">
        Secure by Design
      </h2>
      <p class="text-xl leading-[1.8] opacity-70 max-w-2xl mx-auto">
        Traditional authentication places the policy on the server. ERC-8128 flips the model: the client defines the security posture of every request.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-16">
      <!-- Request Binding -->
      <div class="border border-border-subtle p-7 bg-bg/50">
        <div class="font-mono text-sm opacity-50 uppercase tracking-widest mb-4">Axis 1</div>
        <h3 class="text-2xl font-bold uppercase mb-4">Request Binding</h3>
        <p class="text-base opacity-70 leading-[1.7] mb-6">
          How tightly a signature is bound to a request.
        </p>
        <div class="space-y-4">
          <div class="flex gap-3">
            <div class="w-2 h-2 bg-white rounded-full mt-2 shrink-0"></div>
            <div>
              <span class="font-bold">Request-Bound</span>
              <span class="opacity-70"> — authorizes exactly one HTTP request. Any change to method, path, query, or body fails verification.</span>
            </div>
          </div>
          <div class="flex gap-3">
            <div class="w-2 h-2 bg-white/40 rounded-full mt-2 shrink-0"></div>
            <div>
              <span class="font-bold">Class-Bound</span>
              <span class="opacity-70"> — authorizes a class of requests by selecting which components to sign. Fine-grained control, like scoped bearer tokens.</span>
            </div>
          </div>
        </div>
      </div>

      <!-- Replay Protection -->
      <div class="border border-border-subtle p-7 bg-bg/50">
        <div class="font-mono text-sm opacity-50 uppercase tracking-widest mb-4">Axis 2</div>
        <h3 class="text-2xl font-bold uppercase mb-4">Replay Protection</h3>
        <p class="text-base opacity-70 leading-[1.7] mb-6">
          Whether a signature can be validly reused.
        </p>
        <div class="space-y-4">
          <div class="flex gap-3">
            <div class="w-2 h-2 bg-white rounded-full mt-2 shrink-0"></div>
            <div>
              <span class="font-bold">Non-Replayable</span>
              <span class="opacity-70"> — includes a nonce. Verifiers enforce uniqueness. Guarantees single-use authorization.</span>
            </div>
          </div>
          <div class="flex gap-3">
            <div class="w-2 h-2 bg-white/40 rounded-full mt-2 shrink-0"></div>
            <div>
              <span class="font-bold">Replayable</span>
              <span class="opacity-70"> — may be reused within a specified validity window. Useful for high-frequency, idempotent operations.</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Security Spectrum Graph -->
    <div class="mb-16">
      <div class="max-w-2xl mx-auto">
        <div class="border border-border-subtle bg-bg/50 p-4 sm:p-8 md:p-10">
          <svg viewBox="0 0 500 460" class="w-full select-none overflow-visible" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Security spectrum: request binding vs replayability">
            <defs>
              <linearGradient id="spectrum-grad" x1="100%" y1="0%" x2="0%" y2="100%">
                <stop offset="0%" stop-color="#858aff" stop-opacity="0.1" />
                <stop offset="100%" stop-color="#858aff" stop-opacity="0.01" />
              </linearGradient>
            </defs>

            <!-- Plot area fill -->
            <rect x="55" y="20" width="420" height="400" fill="url(#spectrum-grad)" />

            <!-- Grid lines -->
            <g stroke="rgba(255,255,255,0.04)" stroke-width="0.5">
              <line x1="160" y1="20" x2="160" y2="420" />
              <line x1="370" y1="20" x2="370" y2="420" />
              <line x1="55" y1="120" x2="475" y2="120" />
              <line x1="55" y1="320" x2="475" y2="320" />
            </g>

            <!-- Axes (crossing at center) -->
            <line x1="55" y1="220" x2="475" y2="220" stroke="rgba(255,255,255,0.18)" stroke-width="1" />
            <line x1="265" y1="20" x2="265" y2="420" stroke="rgba(255,255,255,0.18)" stroke-width="1" />

            <!-- Axis arrows -->
            <polygon points="475,217 475,223 485,220" fill="rgba(255,255,255,0.18)" />
            <polygon points="262,20 268,20 265,10" fill="rgba(255,255,255,0.18)" />

            <!-- Y-axis endpoint labels (centered at tips) -->
            <text x="265" y="0" text-anchor="middle" fill="white" font-size="10" font-family="'JetBrains Mono', 'Fira Code', monospace" font-weight="bold" opacity="0.6">REQUEST-BOUND</text>
            <text x="265" y="446" text-anchor="middle" fill="white" font-size="10" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.6">CLASS-BOUND</text>

            <!-- X-axis endpoint labels (centered at tips) -->
            <text x="465" y="210" text-anchor="end" fill="white" font-size="10" font-family="'JetBrains Mono', 'Fira Code', monospace" font-weight="bold" opacity="0.6">NON-REPLAYABLE</text>
            <text x="65" y="210" text-anchor="start" fill="white" font-size="10" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.6">REPLAYABLE</text>

            <!-- Data points -->

            <!-- Baseline: Request-Bound + Non-Replayable (top-right quadrant) -->
            <circle cx="453" cy="42" r="16" fill="none" stroke="#858aff" stroke-width="0.5" opacity="0.15" />
            <circle cx="453" cy="42" r="10" fill="none" stroke="#858aff" stroke-width="1" opacity="0.35" />
            <circle cx="453" cy="42" r="5" fill="#858aff" opacity="0.9" />
            <text x="433" y="36" text-anchor="end" fill="white" font-size="11.5" font-family="'JetBrains Mono', 'Fira Code', monospace" font-weight="bold" opacity="0.9">Baseline</text>
            <text x="433" y="52" text-anchor="end" fill="white" font-size="9" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">Mandatory posture</text>

            <!-- Security-critical: just below baseline (top-right, slightly relaxed) -->
            <circle cx="420" cy="80" r="3.5" fill="white" opacity="0.4" />
            <text x="410" y="72" text-anchor="end" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">Confidential operations</text>

            <!-- Privileged actions: tight + non-replayable (top-right) -->
            <circle cx="385" cy="105" r="3.5" fill="white" opacity="0.4" />
            <text x="370" y="118" text-anchor="end" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">Privileged actions</text>

            <!-- One-time operations: moderate-tight + very non-replayable (top-right) -->
            <circle cx="435" cy="155" r="3.5" fill="white" opacity="0.4" />
            <text x="420" y="152" text-anchor="end" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">One-time operations</text>

            <!-- Idempotent reads: tight + replayable (top-left) -->
            <circle cx="130" cy="100" r="3.5" fill="white" opacity="0.4" />
            <text x="145" y="97" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">Idempotent reads</text>

            <!-- Moderate-trust endpoints: center -->
            <circle cx="280" cy="235" r="3.5" fill="white" opacity="0.4" />
            <text x="290" y="248" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">Moderate-trust endpoints</text>

            <!-- Scoped write batches: loose + non-replayable (bottom-right) -->
            <circle cx="410" cy="305" r="3.5" fill="white" opacity="0.4" />
            <text x="400" y="315" text-anchor="end" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">Scoped write batches</text>

            <!-- Administrative bulk ops: loose + fairly non-replayable (bottom-right) -->
            <circle cx="325" cy="355" r="3.5" fill="white" opacity="0.4" />
            <text x="460" y="370" text-anchor="end" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">Administrative bulk ops</text>

            <!-- Public data feeds: very loose + very replayable (deep bottom-left) -->
            <circle cx="85" cy="385" r="3.5" fill="white" opacity="0.4" />
            <text x="100" y="382" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">Public data feeds</text>

            <!-- Background syncs: loose + replayable (bottom-left) -->
            <circle cx="175" cy="300" r="3.5" fill="white" opacity="0.4" />
            <text x="190" y="297" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">Background syncs</text>

            <!-- High-frequency APIs: replayable + class-bound (bottom-left) -->
            <circle cx="120" cy="340" r="3.5" fill="white" opacity="0.4" />
            <text x="135" y="337" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">High-frequency APIs</text>

            <!-- Polling endpoints: loose + replayable (bottom-left, near axis) -->
            <circle cx="145" cy="275" r="3.5" fill="white" opacity="0.4" />
            <text x="160" y="272" fill="white" font-size="9.5" font-family="'JetBrains Mono', 'Fira Code', monospace" opacity="0.4">Polling endpoints</text>
          </svg>
        </div>
      </div>
      <p class="text-center text-lg opacity-70 max-w-2xl mx-auto mt-8">
        Any compliant verifier accepts <span class="text-white font-semibold">Request-Bound</span> + <span class="text-white font-semibold">Non-Replayable</span> requests. Clients are never forced to weaken their security guarantees unless they choose to.
      </p>
    </div>

    <div class="text-center border-t border-border-subtle pt-12">
        <p class="mb-8 text-center text-lg opacity-70">
          Built on widely adopted web and Ethereum standards.</p>
      <div class="flex items-center justify-center gap-8 flex-wrap">
        <div class="flex flex-col gap-1.5">
          <span class="font-mono text-lg font-bold tracking-[0.02em]">RFC 9421</span>
          <span class="text-[0.8125rem] opacity-50 uppercase tracking-[0.08em]">HTTP Signatures</span>
        </div>
        <div class="w-px h-10 bg-white/20 max-sm:hidden"></div>
        <div class="flex flex-col gap-1.5">
          <span class="font-mono text-lg font-bold tracking-[0.02em]">ERC-191</span>
          <span class="text-[0.8125rem] opacity-50 uppercase tracking-[0.08em]">Signed Messages</span>
        </div>
        <div class="w-px h-10 bg-white/20 max-sm:hidden"></div>
        <div class="flex flex-col gap-1.5">
          <span class="font-mono text-lg font-bold tracking-[0.02em]">ERC-1271</span>
          <span class="text-[0.8125rem] opacity-50 uppercase tracking-[0.08em]">Smart Accounts</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .bg-grid-lg {
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.15) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.15) 1px, transparent 1px);
    background-size: 60px 60px;
  }
</style>
