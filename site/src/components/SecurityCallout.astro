<section class="security-callout">
  <div class="security-inner">
    <div class="security-icon">
      <svg
        width="48"
        height="48"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.5"
        stroke-linecap="round"
        stroke-linejoin="round"
        aria-hidden="true"
      >
        <rect x="3" y="11" width="18" height="11" rx="2" ry="2" />
        <path d="M7 11V7a5 5 0 0 1 10 0v4" />
      </svg>
    </div>
    <h2>Built on Cryptographic Standards</h2>
    <p>
      ERC-8128 uses the same cryptographic signatures that secure Ethereum itself. Every request is
      tamper-proof, timestamped, and verifiable. No passwords to leak, no keys to rotate—just math.
    </p>
  </div>
</section>

<style>
  .security-callout {
    border-bottom: 1px solid var(--border);
    padding: 6rem 2rem;
    position: relative;
    background: var(--surface);
  }

  @media (min-width: 768px) {
    .security-callout {
      padding: 8rem 3rem;
    }
  }

  .security-inner {
    max-width: 48rem;
    margin: 0 auto;
    text-align: center;
  }

  .security-icon {
    margin-bottom: 2rem;
    opacity: 0.4;
    color: white;
    display: flex;
    justify-content: center;
  }

  h2 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: -0.03em;
    margin-bottom: 2rem;
  }

  p {
    font-size: 1.25rem;
    line-height: 1.8;
    opacity: 0.7;
    max-width: 40rem;
    margin: 0 auto;
  }
</style>
