<section class="what-is">
  <div class="what-is-inner">
    <div class="what-is-marker"></div>
    <p class="what-is-body">
      ERC-8128 lets Ethereum accounts authenticate HTTP requests — API calls, webhooks, data
      fetches — across any web service. Your wallet becomes your identity. No signups, no API keys,
      no friction.
    </p>
  </div>
</section>

<style>
  .what-is {
    border-bottom: 1px solid var(--border);
    padding: 5rem 2rem;
    position: relative;
  }

  @media (min-width: 768px) {
    .what-is {
      padding: 6rem 3rem;
    }
  }

  .what-is-inner {
    max-width: 48rem;
    margin: 0 auto;
    text-align: center;
  }

  .what-is-marker {
    width: 3rem;
    height: 3px;
    background: rgba(255, 255, 255, 0.3);
    margin: 0 auto 2.5rem;
  }

  .what-is-body {
    font-size: clamp(1.25rem, 2.5vw, 1.5rem);
    line-height: 1.8;
    opacity: 0.75;
    max-width: 40rem;
    margin: 0 auto;
  }
</style>
