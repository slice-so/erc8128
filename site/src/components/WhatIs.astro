<section class="what-is">
  <div class="what-is-inner">
    <h2>What is ERC-8128?</h2>
    <p class="what-is-body">
      <!-- Marketing is writing this — placeholder for now -->
      ERC-8128 is an Ethereum standard for signing HTTP requests with your onchain identity.
      Instead of API keys or tokens, every request is cryptographically signed—proving who sent it,
      and that it hasn't been tampered with. Simple, secure, and built on open standards.
    </p>
  </div>
</section>

<style>
  .what-is {
    border-bottom: 1px solid var(--border);
    padding: 5rem 2rem;
    position: relative;
  }

  @media (min-width: 768px) {
    .what-is {
      padding: 6rem 3rem;
    }
  }

  .what-is-inner {
    max-width: 48rem;
    margin: 0 auto;
    text-align: center;
  }

  h2 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: -0.03em;
    margin-bottom: 2rem;
  }

  .what-is-body {
    font-size: 1.25rem;
    line-height: 1.8;
    opacity: 0.7;
    max-width: 40rem;
    margin: 0 auto;
  }
</style>
