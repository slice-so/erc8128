<section class="border-b border-border py-20 px-8 md:py-24 md:px-12 relative">
  <div class="prose max-w-3xl mx-auto text-center space-y-8">
    <!-- <div class="w-12 h-[3px] bg-white/30 mx-auto mb-10"></div> -->
    <p class="text-[clamp(1.25rem,2.25vw,1.5rem)] opacity-90 max-w-[40rem] mx-auto">
      Ethereum accounts are identities. ERC-8128 lets those identities authenticate HTTP requests across the open web.
    </p>
    <p class="text-[clamp(1.25rem,2.25vw,1.5rem)] opacity-90 max-w-[40rem] mx-auto">
      No API keys. No signups. No shared secrets.<br/>
      Your address is your credential.
    </p>
    <p class="text-[clamp(1rem,1.75vw,1.125rem)] opacity-60 max-w-[40rem] mx-auto">
      A composable authentication layer that identity standards, agent frameworks, and application protocols can build on top of.
    </p>
  </div>
</section>
