<section class="what-is">
  <div class="what-is-inner">
    <h1>ERC 8128: Ethereum HTTP Authentication Standard</h1>
    <p class="what-is-body">
      ERC-8128 lets Ethereum accounts authenticate HTTP requests — API calls, webhooks, data
      fetches — across any web service. Your wallet becomes your identity. No signups, no API keys,
      no friction.
    </p>
  </div>
</section>

<style>
  .what-is {
    border-bottom: 1px solid var(--border);
    padding: 5rem 2rem;
    position: relative;
  }

  @media (min-width: 768px) {
    .what-is {
      padding: 6rem 3rem;
    }
  }

  .what-is-inner {
    max-width: 48rem;
    margin: 0 auto;
    text-align: center;
  }

  h1 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: -0.03em;
    margin-bottom: 2rem;
  }

  .what-is-body {
    font-size: 1.25rem;
    line-height: 1.8;
    opacity: 0.7;
    max-width: 40rem;
    margin: 0 auto;
  }
</style>
