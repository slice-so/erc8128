---
const features = [
  {
    title: "Request Integrity",
    description:
      "Signatures cryptographically bind to the HTTP request itself. Any tampering with method, path, query, or body causes verification to fail immediately.",
    icon: "shield"
  },
  {
    title: "Universal Identity",
    description:
      "Your Ethereum address is your identity. ERC-8128 turns it into a web-native credential—no middleware, no token exchange, no enrollment. If you have a wallet, you can authenticate.",
    icon: "ethereum"
  },
  {
    title: "No Bearer Credentials",
    description:
      "Eliminate API keys, sessions, and shared secrets. Every request is self-authenticating: signed by the sender, verified by the receiver. Nothing to store, nothing to rotate, nothing to leak.",
    icon: "key-off"
  },
  {
    title: "Agent-Ready",
    description:
      "Purpose-built for machine-to-machine communication, including autonomous systems. AI agents, bots, and services authenticate with onchain identities. Pairs naturally with identity registries like ERC-8004 (Trustless Agents) for verifiable agent-to-agent communication.",
    icon: "bot"
  }
]
---

<section class="border-b border-border">
  <!-- Mobile header - hidden on desktop -->
    <div class="md:hidden px-4 mt-24 md:mt-32 mb-8 md:mb-12">
      <div class="text-xl font-bold uppercase tracking-[0.15em] flex items-center gap-4">
        <span class="block w-4 h-4 bg-white"></span>
        Features
      </div>
    </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4">
  {
    features.map((feature, i) => (
      <div class={`p-10 flex flex-col h-full border-b border-gray-700 transition-colors duration-300 hover:bg-surface-elevated lg:border-b-0 last:border-b-0 ${i < features.length - 1 ? 'lg:border-r lg:border-gray-700' : ''} ${i % 2 === 0 ? 'md:border-r md:border-gray-700 md:max-lg:border-r' : 'md:max-lg:border-r-0'} ${i >= 2 ? 'md:max-lg:border-b-0' : ''}`}>
        <div class="mb-8 opacity-35 text-white transition-opacity duration-300 group-hover:opacity-100 feature-icon">
          {feature.icon === "shield" && (
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" role="img" aria-label="Cryptographic signature verification shield">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              <path d="M9 12l2 2 4-4"/>
            </svg>
          )}
          {feature.icon === "key-off" && (
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" role="img" aria-label="No bearer credentials required">
              <path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"/>
              <line x1="2" y1="2" x2="22" y2="22" stroke-width="2"/>
            </svg>
          )}
          {feature.icon === "ethereum" && (
             <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" role="img" aria-label="Ethereum wallet-based authentication">
              <polygon points="12,2 4,12 12,16 20,12"/>
              <polygon points="4,12 12,22 20,12 12,16"/>
              <line x1="12" y1="2" x2="12" y2="16"/>
            </svg>
          )}
          {feature.icon === "bot" && (
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" role="img" aria-label="Agent-ready authentication">
              <rect x="3" y="11" width="18" height="10" rx="2"/>
              <circle cx="12" cy="5" r="2"/>
              <path d="M12 7v4"/>
              <line x1="8" y1="16" x2="8" y2="16"/>
              <line x1="16" y1="16" x2="16" y2="16"/>
              <circle cx="8" cy="16" r="1" fill="currentColor"/>
              <circle cx="16" cy="16" r="1" fill="currentColor"/>
              <path d="M9 20h6"/>
            </svg>
          )}
        </div>
        <h3 class="text-xl font-bold uppercase mb-4">{feature.title}</h3>
        <p class="text-base opacity-70 leading-[1.7]">{feature.description}</p>
      </div>
    ))
  }
  </div>
</section>

<style>
  div:hover .feature-icon {
    opacity: 1;
  }
</style>
