---
const features = [
  {
    icon: "🔐",
    title: "Request Integrity",
    description:
      "Signatures cryptographically bind to the HTTP request itself. Any tampering with method, path, query, or body causes verification to fail."
  },
  {
    icon: "🔑",
    title: "Eliminate Token Management",
    description:
      "Stop issuing, rotating, and securing API keys. Clients sign requests directly with EOAs or Smart Contract Accounts—no server-issued credentials required."
  },
  {
    icon: "⛓️",
    title: "Built for Onchain Identity",
    description:
      "Use ERC-1271 for smart contract authorization. Enable delegation, session modules, and composable access policies without changing your authentication layer."
  },
  {
    icon: "📐",
    title: "Standards-Based Security",
    description:
      "Built on RFC 9421 (HTTP Message Signatures) with replay protection. Every compliant client can authenticate with every compliant server."
  }
]
---

<section>
  <div class="container">
    <div class="section-label">Why ERC-8128</div>
    <h2 class="section-title">HTTP auth, rebuilt for Ethereum</h2>
    <div class="features-grid">
      {
        features.map((feature) => (
          <div class="feature">
            <div class="feature-icon">{feature.icon}</div>
            <h3>{feature.title}</h3>
            <p>{feature.description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .section-label {
    font-family: var(--mono);
    font-size: 13px;
    color: var(--accent);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 12px;
  }

  .section-title {
    font-size: clamp(24px, 4vw, 32px);
    font-weight: 800;
    letter-spacing: -0.02em;
    margin-bottom: 40px;
    line-height: 1.2;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }

  .feature {
    padding: 24px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
  }

  .feature-icon {
    font-size: 20px;
    margin-bottom: 12px;
  }

  .feature h3 {
    font-size: 15px;
    font-weight: 700;
    margin-bottom: 6px;
    letter-spacing: -0.01em;
  }

  .feature p {
    font-size: 14px;
    color: var(--text-dim);
    line-height: 1.5;
  }

  @media (max-width: 640px) {
    .features-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
