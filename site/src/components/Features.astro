---
const features = [
  {
    title: "Identity-Native Auth",
    description:
      "Your Ethereum address is your identity. ERC-8128 turns it into a web-native credential—no middleware, no token exchange, no enrollment. If you have a wallet, you can authenticate.",
    icon: "identity"
  },
  {
    title: "No Bearer Credentials",
    description:
      "Eliminate API keys, sessions, and shared secrets. Every request is self-authenticating: signed by the sender, verified by the receiver. Nothing to store, nothing to rotate, nothing to leak.",
    icon: "key-off"
  },
  {
    title: "Composable Protocol Layer",
    description:
      "ERC-8128 handles authentication. What you build on top is up to you—authorization, rate limiting, reputation, payments. A minimal, unopinionated foundation for any HTTP-based protocol.",
    icon: "layers"
  },
  {
    title: "Agent-Ready",
    description:
      "Purpose-built for autonomous systems. AI agents, bots, and services authenticate with on-chain identities. Pairs naturally with identity registries like ERC-8004 (Trustless Agents) for verifiable agent-to-agent communication.",
    icon: "bot"
  }
]
---

<section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 border-b border-border">
  {
    features.map((feature, i) => (
      <div class={`p-10 flex flex-col h-full border-b border-border transition-colors duration-300 hover:bg-surface-elevated lg:border-b-0 last:border-b-0 ${i < features.length - 1 ? 'lg:border-r lg:border-border' : ''} ${i % 2 === 0 ? 'md:border-r md:border-border md:max-lg:border-r' : 'md:max-lg:border-r-0'} ${i >= 2 ? 'md:max-lg:border-b-0' : ''}`}>
        <div class="mb-8 opacity-35 text-white transition-opacity duration-300 group-hover:opacity-100 feature-icon">
          {feature.icon === "identity" && (
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" role="img" aria-label="Identity-native authentication">
              <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
              <circle cx="12" cy="7" r="4"/>
              <path d="M12 14l-1.5 1.5"/>
              <path d="M12 14l1.5 1.5"/>
            </svg>
          )}
          {feature.icon === "key-off" && (
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" role="img" aria-label="No bearer credentials required">
              <path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"/>
              <line x1="2" y1="2" x2="22" y2="22" stroke-width="2"/>
            </svg>
          )}
          {feature.icon === "layers" && (
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" role="img" aria-label="Composable protocol layer">
              <polygon points="12 2 2 7 12 12 22 7 12 2"/>
              <polyline points="2 17 12 22 22 17"/>
              <polyline points="2 12 12 17 22 12"/>
            </svg>
          )}
          {feature.icon === "bot" && (
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" role="img" aria-label="Agent-ready authentication">
              <rect x="3" y="11" width="18" height="10" rx="2"/>
              <circle cx="12" cy="5" r="2"/>
              <path d="M12 7v4"/>
              <line x1="8" y1="16" x2="8" y2="16"/>
              <line x1="16" y1="16" x2="16" y2="16"/>
              <circle cx="8" cy="16" r="1" fill="currentColor"/>
              <circle cx="16" cy="16" r="1" fill="currentColor"/>
              <path d="M9 20h6"/>
            </svg>
          )}
        </div>
        <h3 class="text-xl font-bold uppercase mb-4">{feature.title}</h3>
        <p class="text-base opacity-70 leading-[1.7]">{feature.description}</p>
      </div>
    ))
  }
</section>

<style>
  div:hover .feature-icon {
    opacity: 1;
  }
</style>
