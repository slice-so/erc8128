---
const steps = [
  {
    num: "1",
    title: "Client signs",
    description:
      "The client creates an HTTP Message Signature over the request method, path, headers, and body using an Ethereum account."
  },
  {
    num: "2",
    title: "Headers carry signature",
    description:
      "The signature, signer address, chain ID, and nonce are added as standard HTTP headers. No changes to the request body."
  },
  {
    num: "3",
    title: "Server verifies",
    description:
      "The server reconstructs the signed content, recovers the signer, and validates via ecrecover (EOA) or ERC-1271 (smart contract)."
  }
]
---

<section>
  <div class="container">
    <div class="section-label animate-in">How It Works</div>
    <h2 class="section-title animate-in">Three steps, zero trust assumptions</h2>
    <div class="steps stagger-children">
      {
        steps.map((step, i) => (
          <div class="step animate-in">
            <div class="step-num-wrapper">
              <div class="step-num">{step.num}</div>
              {i < steps.length - 1 && <div class="step-connector" />}
            </div>
            <div class="step-content">
              <h3>{step.title}</h3>
              <p>{step.description}</p>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .steps {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .step {
    position: relative;
    display: flex;
    gap: 20px;
    padding-bottom: 36px;
  }

  .step:last-child {
    padding-bottom: 0;
  }

  .step-num-wrapper {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .step-num {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--accent-dim);
    border: 1px solid var(--accent-border);
    border-radius: 12px;
    font-family: var(--mono);
    font-size: 14px;
    font-weight: 600;
    color: var(--accent);
    transition: all 0.3s var(--ease);
  }

  .step:hover .step-num {
    background: rgba(133, 138, 255, 0.15);
    box-shadow: 0 0 16px rgba(133, 138, 255, 0.15);
  }

  .step-connector {
    flex: 1;
    width: 1px;
    margin-top: 8px;
    background: linear-gradient(
      to bottom,
      var(--accent-border),
      var(--border)
    );
    min-height: 20px;
  }

  .step-content {
    padding-top: 6px;
  }

  .step-content h3 {
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 6px;
    letter-spacing: -0.01em;
  }

  .step-content p {
    font-size: 14px;
    color: var(--text-dim);
    line-height: 1.6;
  }
</style>
